{
  "name": "parcel-template",
  "version": "1.0.0",
  "description": "",
  "source": [
    "./src/pages/index.html"
  ],
  "scripts": {
    "test": "jest ./src/tests/*.test.js",
    "watch": "jest --watchAll",
    "dev": "parcel -p 3000 src/pages/*.html",
    "build": "npm run clean && parcel build src/pages/*.html",
    "clean": "rm -rf dist/*.css dist/*.css.map dist/*.js dist/*.js.map dist/*.html"
  },
  "author": "James Spears, V",
  "license": "ISC",
  "devDependencies": {
    "@parcel/babel-preset-env": "^2.12.0",
    "@parcel/transformer-sass": "^2.12.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-transform-stub": "^2.0.0",
    "normalize.css": "^8.0.1",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "pocketbase": "^0.21.1"
  }
}
